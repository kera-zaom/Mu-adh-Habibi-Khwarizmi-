import Link from "next/link";

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex min-h-screen bg-gray-100">
      <aside className="w-64 bg-blue-800 text-white p-6">
        <h1 className="mb-10 text-3xl font-bold">الخوارزمي</h1>

        <nav className="space-y-3">
          <Link href="/admin" className="block rounded-lg p-3 hover:bg-blue-700">
            🏠 الرئيسية
          </Link>

          <Link href="/admin/exams" className="block rounded-lg p-3 hover:bg-blue-700">
            📝 الامتحانات
          </Link>

          <Link href="/admin/questions" className="block rounded-lg p-3 hover:bg-blue-700">
            ❓ الأسئلة
          </Link>

          <Link href="/admin/students" className="block rounded-lg p-3 hover:bg-blue-700">
            👨‍🎓 الطلاب
          </Link>

          <Link href="/admin/results" className="block rounded-lg p-3 hover:bg-blue-700">
            📊 النتائج
          </Link>

          <Link href="/admin/settings" className="block rounded-lg p-3 hover:bg-blue-700">
            ⚙️ الإعدادات
          </Link>
        </nav>
      </aside>

      <div className="flex-1">
        <header className="flex items-center justify-between bg-white p-5 shadow">
          <h2 className="text-2xl font-bold">لوحة تحكم المستر</h2>

          <button className="rounded-lg bg-red-600 px-4 py-2 text-white">
            تسجيل الخروج
          </button>
        </header>

        <main className="p-6">
          {children}
        </main>
      </div>
    </div>
  );
}
